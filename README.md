# GitHub User Activity CLI (PHP)

A simple PHP command-line tool to fetch and display recent GitHub activity of a user.

## Features
- Accepts GitHub username from CLI
- Fetches recent activity using GitHub API
- Displays latest events: Push, Create, Branch, Issues, etc.
- Handles missing data safely
https://roadmap.sh/projects/github-user-activity
